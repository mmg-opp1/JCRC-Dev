<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Member_Type__c</fullName>
    <description>The member's type on the board (JCRC Recruited / UJA Appointed / UJA Observer). Sets the term length (auto-fills End Date + generates the per-year records) and the base annual commitment. JSI-123 / Jason Ott.</description>
    <label>Member Type</label>
    <referenceTo>Board_Member_Type__c</referenceTo>
    <relationshipLabel>Board Terms</relationshipLabel>
    <relationshipName>Board_Terms</relationshipName>
    <deleteConstraint>Restrict</deleteConstraint>
    <required>false</required>
    <type>Lookup</type>
</CustomField>
